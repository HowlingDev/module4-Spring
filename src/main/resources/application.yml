spring:
  datasourse:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: gvolk
  jpa:
    hibernate:
      ddl-auto=update
    show-sql:
      true
    database-platform:
      org.hibernate.dialect.PostgreSQLDialect